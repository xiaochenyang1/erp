spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${ERP_DATASOURCE_URL}
    username: ${ERP_DATASOURCE_USERNAME}
    password: ${ERP_DATASOURCE_PASSWORD}
    hikari:
      maximum-pool-size: ${ERP_DB_POOL_MAX_SIZE:20}
      minimum-idle: ${ERP_DB_POOL_MIN_IDLE:5}
      connection-timeout: ${ERP_DB_CONNECTION_TIMEOUT_MS:30000}
      validation-timeout: ${ERP_DB_VALIDATION_TIMEOUT_MS:5000}
  data:
    redis:
      host: ${ERP_REDIS_HOST}
      port: ${ERP_REDIS_PORT:6379}
      password: ${ERP_REDIS_PASSWORD}
      timeout: ${ERP_REDIS_TIMEOUT:5s}
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: ${ERP_FLYWAY_BASELINE_ON_MIGRATE:false}
  lifecycle:
    timeout-per-shutdown-phase: 30s

server:
  shutdown: graceful
  forward-headers-strategy: framework
  compression:
    enabled: ${ERP_HTTP_COMPRESSION_ENABLED:true}
    min-response-size: ${ERP_HTTP_COMPRESSION_MIN_RESPONSE_SIZE:2KB}
    mime-types: application/json,application/problem+json,text/plain,text/html,text/css,application/javascript
  tomcat:
    threads:
      max: ${ERP_TOMCAT_THREADS_MAX:200}
      min-spare: ${ERP_TOMCAT_THREADS_MIN_SPARE:10}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  health:
    redis:
      enabled: true

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

erp:
  error:
    expose-unexpected-message: false
  attachment:
    storage-root: ${ERP_ATTACHMENT_STORAGE_ROOT:/var/lib/erp-server/attachments}
    max-file-size-bytes: ${ERP_ATTACHMENT_MAX_FILE_SIZE_BYTES:20971520}
  idempotency:
    enabled: ${ERP_IDEMPOTENCY_ENABLED:true}
    ttl-seconds: ${ERP_IDEMPOTENCY_TTL_SECONDS:86400}
    max-replay-body-bytes: ${ERP_IDEMPOTENCY_MAX_REPLAY_BODY_BYTES:1048576}
    max-request-body-bytes: ${ERP_IDEMPOTENCY_MAX_REQUEST_BODY_BYTES:1048576}
  import:
    max-file-size-bytes: ${ERP_IMPORT_MAX_FILE_SIZE_BYTES:5242880}
    max-rows: ${ERP_IMPORT_MAX_ROWS:5000}
    max-cell-length: ${ERP_IMPORT_MAX_CELL_LENGTH:4096}
    commit-batch-size: ${ERP_IMPORT_COMMIT_BATCH_SIZE:500}
  report:
    max-export-rows: ${ERP_REPORT_MAX_EXPORT_ROWS:5000}
    export-batch-size: ${ERP_REPORT_EXPORT_BATCH_SIZE:500}
  security:
    public-api-docs-enabled: false
    trusted-proxies: ${ERP_TRUSTED_PROXIES:}
    principal-cache-invalidation:
      mode: ${ERP_PRINCIPAL_CACHE_INVALIDATION_MODE:redis}
    cors:
      allowed-origins: ${ERP_CORS_ALLOWED_ORIGINS:}
    jwt:
      secret: ${ERP_JWT_SECRET}
      access-token-ttl-seconds: ${ERP_JWT_ACCESS_TOKEN_TTL_SECONDS:7200}
      refresh-token-ttl-seconds: ${ERP_JWT_REFRESH_TOKEN_TTL_SECONDS:1209600}
