# ERP 未完成功能清单（执行板）

**路径**: `docs/未完成.md`  
**更新时间**: 2026-08-19（执行模式持续推进）
**执行约定**: 可编码项逐项实现并标 `DONE`；人签/运维/边界外 **不伪完成**。

**状态**: `TODO` | `DOING` | `DONE` | `BLOCKED_HUMAN` | `OPTIONAL` | `WONT`

---

## 进度总览

| 分类 | DONE | 阻塞/不做 | 剩余 TODO |
|------|------|-----------|-----------|
| P0 发布 | 0 | 5 | 0 可编码 |
| P1 业务 | **15**（P1 可编码项已齐） | 0 | 0 |
| P2 体验 | **10** | 0 | 0 |
| 工程债 | **4** | 0 | 1 |
| 边界外 | — | 8 WONT | — |

---

## 1. P0 · 发布阻塞

| ID | 项 | 状态 |
|----|----|------|
| P0-1 | 财务签字 | **BLOCKED_HUMAN** |
| P0-2 | 质检签字 | **BLOCKED_HUMAN** |
| P0-3 | 运维监控上线 | **BLOCKED_HUMAN** |
| P0-4 | 独立机房预生产 | **OPTIONAL** |
| P0-5 | 正式发布 tag | **OPTIONAL** |

---

## 2. P1 · 业务功能

### 2.1 业务单据打印 · **DONE**
- 销售订单/发货、采购订单/收货浏览器打印  
- 证据：前端 `utils/bizPrint.ts` + 列表「打印」按钮；commit `647dbfb`

### 2.2 过程检 IPQC · **DONE**
- 类型 IPQC + production_order_id；成品需检时完工闸门；前端可选 IPQC
- V115；commit 本批

### 2.3 轻量计划 MRP-lite · **DONE**
- `POST /api/inventory/mrp/run`：销售未发货+安全库存需求，BOM展开，输出采购/生产建议；页 `/inventory/mrp`；V117

### 2.4 发票关联增强 · **DONE**
- 关联 PO/SO 须同时填类型+ID，并校验单据存在

### 2.5 收付核销 UX · **DONE**
- 收款/付款支持多选未结单 + 自动分摊 + 未核销提示

### 2.6 审批超时/转签 · **DONE**
- 待办可转签给其他用户，写 TRANSFER 记录+通知
- 审批任务支持截止时间和逾期标识，逾期后可升级给其他有效用户，并写 ESCALATE 记录+通知
- 每个业务类型可配置 1–720 小时审批时限，新任务及升级后的截止时间按当前账套配置计算
- 审批待办支持“仅看超时”筛选，后端按当前账套、待审批状态和截止时间过滤

### 2.7 往来对账单 · **DONE**
- `GET /api/finance/statements`；页 `/finance/statements`；V114  
- 客户：应收发生 + 收款；供应商：应付 + 付款

### 2.8 批次强制与追溯 · **DONE（入库强制，出库自动拣选）**
- 商品 `lotControlled=1` 时入库过账强制批号；出库可显式指定批号，未指定时按 FEFO/FIFO 自动拆批并逐批写库存流水
- 新增 `GET /api/inventory/lots/genealogy` 与 `/inventory/lot-genealogy`：支持批次上下游双向谱系、采购供应商/销售客户解析、跨生产工单连接、单据深链、客户召回清单导出
- 支持 `maxDepth`、单层/总节点上限、循环检测及数据范围受限提示；新增 V145 菜单与 `inventory:lot:genealogy` 权限

### 2.9 销售报价单 · **DONE**
- V116：DRAFT→确认→转销售订单草稿/作废；页 `/sales/quotes`

### 2.10 毛利简报 · **DONE**
- `GET /api/finance/gross-margin`；页 `/finance/gross-margin`；成本=采购价×销量（近似）

### 2.11 客户信用额度概览 · **DONE**
- 客户详情展示未结应收、未发货已审批订单、已用额度、可用额度及超限状态
- `GET /api/masterdata/customers/{id}/credit-exposure` 复用销售审批信用敞口口径

### 2.12 商品库存概览 · **DONE**
- 商品详情展示跨仓现存、预占、可用数量、库存金额及有库存仓库数
- `GET /api/masterdata/products/{id}/stock-summary` 按当前公司和账套汇总

### 2.13 供应商应付敞口概览 · **DONE**
- 供应商详情展示未结应付、未收货采购承诺及应付敞口合计
- `GET /api/masterdata/suppliers/{id}/payable-exposure` 按当前公司和账套汇总

### 2.14 仓库库存概览 · **DONE**
- 仓库详情展示库存 SKU、现存、预占、可用数量及库存金额
- `GET /api/masterdata/warehouses/{id}/stock-summary` 按当前公司和账套汇总

### 2.15 销售订单授信预警 · **DONE**
- 销售订单编辑页展示信用额度、未结应收、在途订单敞口、本单含税金额、提交后敞口与可用额度
- `POST /api/sales/orders/credit-preview` 复用审批信用口径做前端预览
- 提交审批前先校验信用额度，超限直接拒绝；审批通过仍保留复检

---

## 3. P2 · 体验

| ID | 项 | 状态 |
|----|----|------|
| P2-1 | 看板嵌入账龄卡片、审批超时预警及 TOP SKU | **DONE** |
| P2-2 | 数据范围 smoke 链路 | **DONE** |
| P2-3 | 通知批量已读 | **DONE** |
| P2-4 | 导入错误友好展示 | **DONE** |
| P2-5 | 附件强制留痕 | **DONE**（15 类单据全部挂闸门 + 启动校验 + 覆盖门禁；默认仍只开 EXPENSE） |
| P2-6 | 单据复制 SO/PO | **DONE** |
| P2-7 | Web 扫码录入 | **DONE**（产品条码持久化 + 摄像头/扫码枪计数；2 条 ui-smoke workflow 通过） |
| P2-8 | 多语言/时区 | **DONE**（语言/时区偏好已入库并前后端同步；全部页面模板已清除中文 UI 硬编码，业务页面文案、按钮、状态、校验、反馈及文件名完成中英文迁移；金额/数字/百分比/日期时间统一按用户偏好格式化；业务日期按用户业务时区生成；页面键完整性、英文值及硬编码扫描均有自动化回归） |
| P2-9 | 新用户引导 | **DONE** |
| P2-10 | OpenAPI 生成类型 | **DONE**（产品模块试点：版本化契约 + 生成类型 + CI 漂移检查） |

---

## 4. 工程债

| ID | 项 | 状态 |
|----|----|------|
| E-1 | 巨石 Service/Vue 拆分 | **DOING**（库存查询页八个 composable，后端已将批次库存列表/详情、批次追溯和效期预警抽离为 `InventoryLotQueryService`；生产订单页七个 composable；采购收货页三个 composable，后端已抽离查询/数据权限/导出与过账编排两个 Service；采购订单页三个 composable；采购订单后端已抽离查询/数据权限/导出与全链路追踪两个 Service；商品/客户/供应商/仓库管理页各三个 composable（含表单）；财务期间页两个 composable；采购退货页三个 composable，后端已抽离查询/数据权限/导出与过账编排两个 Service；销售发货页三个 composable，后端已抽离查询/数据权限与过账编排两个 Service；销售订单页三个 composable，后端已抽离查询/数据权限读侧 Service；销售退货页三个 composable，后端已抽离查询/数据权限与过账编排两个 Service；采购询价页四个 composable（展示/列表/表单/报价），后端已抽离报价校验/持久化 Service；库存预警页已抽离展示映射与列表/规则/处置动作 composable，后端已抽离规则筛选/余额水合/处置状态叠加查询 Service；库存盘点页三个 composable（展示/列表/表单）；库存调拨页三个 composable（展示/列表/表单）；库存调整页三个 composable（展示/列表/表单）；质量检验页四个 composable（展示/列表/建单/编辑判定），后端已抽离三类建单 Service、来源访问组件与查询/账套隔离/导出 Service；收付款页四个共享 composable（展示/列表/表单/详情，收付双向复用）；费用管理页三个 composable（展示/列表/表单），后端已抽离查询/账套隔离/凭证对账与过账/红冲编排两个 Service；手工凭证页三个 composable（展示/列表/表单），后端已抽离筛选分页/账套隔离/分录水合/DTO 映射查询 Service；资金对账页三个 composable（展示/列表/表单，账户与银行流水双标签页）；生产 BOM 页三个 composable（展示/列表/表单）；工艺路线页三个 composable（展示/列表/表单）；请购单页三个 composable（展示/列表/表单）；异常工单页三个 composable（展示/列表/表单动作），后端已抽离筛选分页/账套隔离/事件水合/追踪映射查询 Service；异常规则页三个 composable（展示/列表扫描/配置表单）；异常 SLA 策略页三个 composable（展示/列表/配置表单）；系统用户页三个 composable（展示/列表启停/表单角色与数据范围）；补货建议页三个 composable（展示/列表动作/编辑表单），后端已抽离筛选分页/账套隔离/展示水合/履约状态映射查询 Service；MRP 页已抽离历史运行查询、建议行分组/商品展示水合与公司账套守卫为 `MrpPlanQueryService`，并抽离需求收集、库存/在途/在制供给抵扣和 BOM 展开的只读 `MrpPlanCalculationService`，运行结果持久化与转采购/生产订单写编排继续留在门面；报表中心后端已将采购/销售订单报表、库存余额/库存流水、应收/应付结算报表分别抽离为 `OrderReportQueryService`、`InventoryReportQueryService`、`FinanceSettlementReportQueryService`，分别承担筛选、租户/数据范围、DTO 映射和导出批处理，`ReportQueryService` 仅保留兼容委托门面；业务追溯后端已抽离无 Mapper/用户上下文依赖的 `BusinessTraceAssemblyService`，负责文档、时间线、异常工单和汇总组装，`BusinessTraceService` 保留租户化检索与业务编号扩散；工作中心页三个 composable（展示/列表/表单）；预生产验收页三个 composable（展示/列表详情/多对话框表单）；系统角色页三个 composable（展示/列表启停/表单菜单与数据范围）；系统日志页两个 composable（展示/操作登录审计三标签列表）；系统配置页三个 composable（展示/配置与编号规则列表/双表单）；系统字典页三个 composable（展示/类型与字典项列表/双表单）；工作台两个 composable（展示/数据加载，图表留页面）；单据追踪页两个 composable（展示/检索与业务动态）；初始数据导入页两个 composable（展示/任务列表预览提交）；审批待办页两个 composable（展示/列表含通过驳回转签升级）；审批配置页两个 composable（展示/节点审批人表单）；销售价目页两个 composable（展示/列表启停与表单）；采购价目页两个 composable（展示/列表启停与表单）；销售报价页三个 composable（展示/列表动作/表单明细）；发票登记页三个 composable（展示/列表确认作废/表单）；会计科目页三个 composable（展示/树列表启停/表单）；序列号台账页三个 composable（展示/列表出库报废/建档表单）；总账明细账页两个 composable（展示合计/总账明细查询导出）；系统岗位页三个 composable（展示/列表启停/表单）；系统部门页三个 composable（展示/树列表启停/表单）；系统菜单页三个 composable（展示/树列表启停/表单）；报表中心页两个 composable（展示/五类报表列表与导出）；附件中心页三个 composable（展示/列表/上传表单）；应收应付共享页三个 composable（展示/双实例列表/权限资源）；通知中心页两个 composable（展示/列表及已读动作）；凭证查询页两个 composable（展示/列表详情打印）；其余大 Service/Vue 按模块继续） |
| E-2 | 缓存深化 | **DONE**（字典已有 + 菜单缓存） |
| E-3 | 前端测试加厚 | **DONE** |
| E-4 | 新功能进 smoke | **DONE** |
| E-5 | 热路径索引 | **DONE**（V118） |

- E-1 后端最新进展：采购收货与采购退货均已抽离查询/数据权限/导出和过账编排两个 Service；异常规则已抽离扫描发现、活动工单去重、工单创建、命中回写、扫描状态与到期调度编排 Service；审批工作流已抽离查询/数据权限/DTO 映射查询 Service（写编排留在门面）；销售订单已抽离查询/数据权限/DTO 映射读侧 Service（写编排留在门面）；质量检验已抽离筛选分页、账套隔离、明细加载、CSV 导出与 DTO 映射查询 Service（建单/编辑/提交/判定/作废留在门面）；异常工单已抽离筛选分页、账套隔离、事件批量水合、追踪链接与 DTO 映射查询 Service（编号/通知/状态机/超时升级留在门面）；手工凭证已抽离筛选分页、账套守卫、租户化分录加载及生命周期 DTO 映射查询 Service（保存/提交/审批/过账/红冲/删除留在门面）；补货建议已抽离筛选分页、账套守卫、仓库/商品/供应商/采购单批量展示水合、履约状态与 DTO 映射查询 Service（创建/编辑/取消/转采购订单留在门面）；费用管理已抽离筛选分页、账套守卫、原凭证/红冲凭证与分录水合、余额核对及 DTO 映射查询 Service，以及附件/期间/科目闸门、原凭证和红冲凭证幂等写入、借贷分录写入与费用头更新的过账 Service（创建/编辑/审批/驳回/作废留在门面）；库存预警已抽离规则筛选、公司账套隔离、库存余额/仓库/商品批量水合、处置状态叠加与缺口恶化重新激活、DTO 映射查询 Service（规则维护、忽略/已处理和重新激活留在门面）；MRP 已抽离历史运行筛选分页、计划/建议行租户守卫、采购/生产建议分组、商品展示水合及 DTO 映射查询 Service，并抽离需求收集、库存/在途/在制供给抵扣和 BOM 展开的只读计算 Service（结果持久化和转采购/生产订单仍留在门面）；报表中心已将采购/销售订单、库存余额/流水、应收/应付结算读侧分别抽离为 `OrderReportQueryService`、`InventoryReportQueryService`、`FinanceSettlementReportQueryService`，兼容门面仅负责原公开方法委托，三类协作者均保留公司/账套或财务数据范围、DTO 映射和导出批处理；业务追溯已将六类文档映射、九类时间线映射与排序、异常工单链接和汇总统计抽离至纯组装 `BusinessTraceAssemblyService`，租户化查询、关键词归一化与已知业务编号扩散仍留在 `BusinessTraceService`；库存查询已将批次、余额和流水读侧分别抽离至 `InventoryLotQueryService`、`InventoryBalanceQueryService`、`InventoryTransactionQueryService`，三个协作者保留公司/账套、数据范围、分页筛选、详情守卫、精度/DTO 映射及余额导出身份恢复，`InventoryStockQueryService` 仅保留原公开接口的兼容委托。

- E-1 审批配置后端：新增 `WorkflowApprovalConfigQueryService`，承接配置读取与响应批量装配、活动节点流转、USER/ROLE 审批人展开、工作流查看权限兜底、审批权限和超时时限解析；`WorkflowApprovalConfigService` 只保留配置保存、节点/审批人替换和原运行时方法兼容委托，从 532 行降至 227 行、构造依赖从 9 个降至 5 个，控制器与工作流调用接口不变。

- E-1 系统日志后端：新增 `SystemLogQueryService`，承接登录/操作/审计日志的租户化筛选分页、操作日志详情守卫、DTO 映射和 CSV 流式导出；`SystemLogService` 保留登录、操作与审计日志写入以及原查询接口兼容委托，从 527 行降至 261 行，控制器和各业务审计调用方接口不变。

- E-1 采购询价后端：新增 `PurchaseInquiryQueryService`，承接列表筛选分页、公司/账套守卫、询价与报价明细水合、响应映射及采购订单预填；`PurchaseInquiryService` 保留创建/编辑/提交/报价/选标/转采购订单/作废写编排和原查询接口兼容委托，从 547 行降至 382 行，逐行报价校验与持久化继续复用 `PurchaseInquiryQuoteService`，控制器接口不变。

- E-1 通知中心后端：新增 `NotificationQueryService`，承接当前用户通知的租户化筛选分页、未读计数、通知头批量水合、接收人/通知守卫与响应映射；`NotificationService` 保留已读状态更新、工作流通知创建/关闭、业务通知和 webhook 发布及原查询接口兼容委托，从 490 行降至 373 行，控制器和工作流调用接口不变。

- E-1 预生产验收后端：已将运行单筛选分页、公司/账套守卫、验收项与证据批量水合及三类响应映射抽离为 `ReadinessQueryService`；创建运行单、登记验收项/证据、记录人工结果、预检证据和 Go/No-Go 决策仍留在写门面，人工签字和运维证据状态不变。

- E-1 库存预占运营后端：已将列表/详情/来源追踪/汇总与事件水合抽离为 `InventoryReservationQueryService`，将数量、余额、负可用量和销售来源一致性检查抽离为 `InventoryReservationCheckService`；`InventoryReservationOpsService` 仅保留人工释放、销售出库草稿占用保护、审计日志和兼容委托入口，来源追踪事件由逐条查询优化为一次批量水合。

---

## 5. 边界外 · WONT

多账套 SaaS、多币种、自由 BPM、固定资产/合并报表、税务申报、移动端全功能、复杂 BI、完整 APS — **WONT**。

---

## 6. 完成记录

| 时间 | 项 | 证据 |
|------|----|------|
| 2026-08-19 | E-1 库存效期预警列表前端拆分 | 新增 `useInventoryStockExpiryAlertList`，承接效期预警弹窗的范围继承、批次号/预警天数/状态筛选、重置、独立 page/size 分页和列表加载，并用请求序号避免快速分页旧响应或旧失败覆盖最新状态；从库存查询页的 queries/resources/actions 移除效期预警职责。新增 6 个独立测试；前端全量测试、lint、类型检查、契约检查、生产构建及 `git diff --check` 全部通过，保留原 API、错误键和契约入口 |
| 2026-08-19 | E-1 库存流水列表/详情前端拆分 | 新增 `useInventoryStockTransactionList`，承接流水查询弹窗的范围继承、筛选、重置、独立 page/size 分页、列表加载及流水详情加载；从 `useInventoryStockQueries`、`useInventoryStockResources`、`useInventoryStockActions`、`useInventoryStockDetails` 移除重复职责，详情 loading 与列表 loading 分离，并用请求序号避免快速分页或连续查看详情时旧响应覆盖新结果。新增 9 个独立测试；前端全量 216 文件 878 项测试、lint、类型检查、契约检查、生产构建及 `git diff --check` 全部通过 |
| 2026-08-19 | E-1 库存余额主列表前端拆分 | 新增 `useInventoryStockBalanceList`，从库存查询页抽离余额列表加载、仓库/商品/库位选项、筛选重置、分页与 CSV 导出；`useInventoryStockResources` 仅保留六类弹窗资源，页面分页改为独立 page/size 处理器并保持选项分页、库位 ID 兼容过滤与失败反馈语义。新增 5 个独立测试，前端全量 215 文件 871 项测试、lint、类型检查、契约检查、生产构建及 `git diff --check` 全部通过 |
| 2026-08-19 | E-1 库存余额/流水查询 Service 拆分 | 新增 `InventoryBalanceQueryService` 与 `InventoryTransactionQueryService`，从 `InventoryStockQueryService` 抽离库存余额列表/详情/CSV 导出和库存流水列表/详情，保留公司/账套隔离、库存数据范围、筛选归一化、分页上限、数量精度、导出线程认证上下文与详情守卫；原门面降为余额/流水/批次查询兼容委托，Controller 与 API 签名不变。新增流水查询行为测试并扩展租户边界、依赖方向和只读事务门禁，专项 19 项及后端编译通过；依赖默认 MySQL 的控制器导出测试仍受本机 `root` 认证阻塞 |
| 2026-08-17 | P2-5 附件强制留痕闸门收口 | `AttachmentBusinessType` 此前声明 15 个类型为 GATED，实际只有 EXPENSE（+ 工作区未提交的 MANUAL_VOUCHER）真正调用 `requireIfConfigured`，其余 13 个配进 `erp.attachment.required-business-types` 后静默不生效；javadoc 点名的 `AttachmentGateCoverageTest` 与 `AttachmentRequiredTypeValidator` 均不存在。本次把 13 个类型全部接线到持有写事务的生命周期方法（`FinanceInvoiceService.post`、`SalesOrderService.submit`、`SalesDeliveryPostingService.post`、`SalesReturnPostingService.post`、`PurchaseRequisitionService.submit`、`PurchaseOrderService.submit`、`PurchaseReceiptPostingService.post`、`PurchaseReturnPostingService.post`、`InventoryAdjustmentService.post`、`InventoryTransferService.post`、`InventoryStockCheckService.postAdjustment`、`QcInspectionService.submit`、`ProductionOrderService.release`），统一放在幂等短路和状态校验之后、第一次写入之前，因此已过账单据仍幂等返回、状态错误优先级不变；Expense 两处裸字符串改用常量并把闸门移到状态翻转前，15 个类型语义一致。新增 `AttachmentRequiredTypeValidator`（`InitializingBean`，配置出现非 GATED 类型即在上下文刷新期启动失败并列出违规值与合法清单）与 `AttachmentGateCoverageTest`（扫描 `src/main/java` 校验 GATED、常量与真实调用点三向一致），把「静默不生效」变为「起不来 / 构建红」。校验器最初实现为 `ApplicationRunner`，真实启动实验发现它在 Tomcat 已监听 8080、日志已打印 `Started ErpServerApplication` 之后才抛错，存在一段对外服务但管控未生效的窗口，遂改为 `InitializingBean` 并补一条断言接口选择的回归；改后坏配置下 Tomcat 与应用均不再启动，退出码 1。13 个逐类型零写入行为测试断言闸门抛错时状态不变、`accountPeriodGuard` 未被触达、主 Mapper 与下游过账零调用；`PurchaseRequisitionService` 此前无任何测试，新建 `PurchaseRequisitionServiceAttachmentGateTest`。变异验证：删除 SALES_DELIVERY 闸门 → 行为测试与覆盖门禁双双失败；仅将 PURCHASE_RECEIPT 闸门下移一行至 `accountPeriodGuard` 之后 → 行为测试失败而覆盖门禁不报错，证明行为测试校验的是位置而非存在性。真实 MySQL 8.4 全量 1433 项测试 0 失败 0 错误、2 项按配置跳过（基线 1407 项，净增 26 项：覆盖门禁 3 + 启动校验 7 + 请购单新建 2 + 既有文件逐类型零写入 12 + 工作区原有手工凭证 2）；另做真实启动 A/B 实验：`EXPENSE,PAYMENT` 启动失败且 Tomcat 未监听，`EXPENSE,PURCHASE_ORDER,QC_INSPECTION` 正常启动。`application.yml` 补齐可选值清单注释。默认配置值不变（仍只强制 EXPENSE），本次交付的是「配置可信」而非「打开管控」。前端未在 15 个单据页内嵌上传入口，附件中心 `/system/attachments` 可按业务类型+业务 ID 上传，被拦单据不是死路，页内入口列为独立需求。P0 人工签字和运维项状态保持不变 |
| 2026-08-14 | E-1 通知中心查询 Service 拆分 | 新增 `NotificationQueryService`，从 `NotificationService` 抽离当前用户通知列表的空查询默认分页、200 条上限、公司/账套/用户过滤、类别与类型归一化、活动通知子查询、通知头批量水合、未读计数、接收人与通知租户守卫及 DTO 映射；门面保留单条/批量/全部已读更新、工作流待办通知创建与关闭、审批结果/业务通知创建、webhook 失败隔离和两个原查询兼容入口，从 490 行降至 373 行，控制器与异常工单/工作流调用接口不变。查询行为、依赖方向与统一只读/写事务专项 11 项通过；隔离 MySQL 8.4 下通知控制器真实列表/未读/已读更新、工作流通知、webhook、迁移契约和 Spring 装配相关回归 34 项通过；后端 996 个源文件编译和 `git diff --check` 通过。P0 人工签字和运维项状态保持不变 |
| 2026-08-14 | E-1 采购询价查询 Service 拆分 | 新增 `PurchaseInquiryQueryService`，从 `PurchaseInquiryService` 抽离空查询默认分页、公司/账套与逻辑删除守卫、询价明细加载、报价及报价行水合、汇总/详情 DTO 映射和采购订单预填；门面保留创建/编辑/提交、录入与选定报价、原子转换采购订单、作废等 REQUIRED 写事务，以及三个原只读接口兼容委托，从 547 行降至 382 行；逐行报价价格解析与完整性校验继续复用 `PurchaseInquiryQuoteService`，未复制报价持久化逻辑。新增 3 个查询行为测试并扩展 3 个依赖/事务结构测试，专项单元与统一事务门禁 33 项通过；隔离 MySQL 8.4 下采购询价命令/查询、控制器权限、逐行报价与原子转换迁移契约及真实全量 Spring Bean 装配相关回归 47 项通过；后端 995 个源文件编译和 `git diff --check` 通过。P0 人工签字和运维项状态保持不变 |
| 2026-08-14 | E-1 系统日志查询 Service 拆分 | 新增 `SystemLogQueryService`，从 `SystemLogService` 抽离登录/操作/审计日志的空查询默认分页、200 条上限、公司/账套过滤、筛选归一化、倒序排序、三类 DTO 映射、操作日志详情跨账套拒绝，以及保留调用线程认证上下文的 CSV 流式导出；门面保留登录成功/失败、操作日志和业务审计日志写入及五个原查询/导出兼容入口，从 527 行降至 261 行，控制器、认证、编号规则、工作流记录和库存预占调用接口不变。查询/导出/结构与统一只读事务专项 15 项通过；真实 MySQL 8.4 下系统日志租户隔离、导出控制器、编号规则审计写入及相关调用方回归 34 项通过；后端 994 个源文件编译和 `git diff --check` 通过。P0 人工签字和运维项状态保持不变 |
| 2026-08-14 | E-1 审批配置查询 Service 拆分 | 新增 `WorkflowApprovalConfigQueryService`，抽离租户化配置读取、节点/审批人响应批量装配、首个/下个活动节点解析、USER/ROLE 审批人展开、提交人和无效/跨账套用户过滤、工作流查看权限兜底、ALL 模式、审批权限和任务超时解析；门面保留 REQUIRED 配置写事务、节点/审批人替换及兼容委托，主 Service 从 532 行降至 227 行、构造依赖从 9 个降至 5 个，原控制器和运行时公开方法不变。新增 4 个查询行为测试、3 个依赖/事务结构测试并接入统一只读事务门禁；专项纯单元/结构回归 25 项通过，真实 MySQL 8.4 下审批配置/工作流控制器、通知和 Spring 装配回归 18 项通过；后端编译与 `git diff --check` 通过。P0 人工签字和运维项状态保持不变 |
| 2026-08-14 | E-1 当前工作树全量回归收口 | 前端清理 CSV BOM 字面字符及未使用导入后，`lint` 0 错误/0 警告、类型检查、211 文件 861 项测试、契约检查与生产构建全部通过；后端修复 V131 库位化后的迁移断言、默认 `MAIN` 库位测试夹具、应收应付到期日导出基线，以及 MySQL/H2 表名大小写导致的资金测试清理失效；真实 MySQL 8.4 最终全量 1388 项测试 0 失败、0 错误、2 项按配置跳过，`git diff --check` 通过。P0 财务/质检签字、运维监控上线等人工项状态保持不变 |
| 2026-08-14 | E-1 库存预占查询/检查 Service 拆分 | 新增 `InventoryReservationQueryService` 与 `InventoryReservationCheckService`，分别承接预留列表、详情、来源追踪、汇总、事件水合，以及预留数量自洽、余额缺失/不一致、负可用量、销售订单来源租户与状态检查；`InventoryReservationOpsService` 从 623 行降至 157 行，只保留人工释放事务、销售出库草稿占用保护和审计日志，控制器原六个接口不变；来源追踪从每条预留单独加载事件优化为一次 `IN` 批量查询。5 个查询、4 个检查、7 个既有租户/释放和 2 个依赖/事务结构测试通过，含事务门禁的专项共 24 项、库存/报表/追溯/Readiness 联合回归 83 项通过；临时 H2（关闭 Flyway）Spring Bean/控制器上下文 2 项通过，后端编译与 `git diff --check` 通过；依赖默认 MySQL 的库存批次过账集成 15 项因本机 `root` 凭据不可用未执行成功 |
| 2026-08-14 | E-1 预生产验收查询 Service 拆分 | 新增 `ReadinessQueryService`，从 `ReadinessService` 抽离运行单筛选分页、公司/账套与逻辑删除守卫、详情验收项加载、证据批量水合及运行单/验收项/证据 DTO 映射；写门面保留默认清单生成、验收项和证据登记、P0/P1 结果门禁、迁移预检证据及 Go/No-Go 决策，人工签字与运维证据未被自动完成。主 Service 从 656 行降至 474 行；5 个查询协作者测试和 2 个依赖/事务结构测试通过，含系统只读事务门禁的 E-1 联合回归 54 项通过；一次性 H2（Flyway 目标 V76，并补当前用户模型四个兼容列）控制器真实 SQL/权限/状态机回归 10 项通过；后端编译与 `git diff --check` 通过 |
| 2026-08-14 | E-1 库存批次查询 Service 拆分 | 新增 `InventoryLotQueryService`，从 `InventoryStockQueryService` 抽离批次库存列表/详情、批次流水追溯、效期预警、公司/账套与库存数据范围、查询归一化与通配符转义、分页上限、数量金额精度、效期状态和来源单据链接映射；主 Service 从 666 行降至 348 行并保留四个只读事务兼容入口。5 个协作者测试与 2 个依赖结构测试新增/通过，连同租户边界和只读事务门禁共 19 项通过；E-1 联合回归 42 项通过，临时 H2（关闭 Flyway）原有批次 SQL 集成 11 项、控制器导出契约 2 项通过；后端编译与 `git diff --check` 通过。真实 MySQL 集成因本机 `root` 凭据不可用未执行 |
| 2026-08-13 | E-1 业务追溯组装 Service 拆分 | 新增无 Mapper、无 `CurrentUserContext` 的 `BusinessTraceAssemblyService`，从 `BusinessTraceService` 抽离六类文档 DTO、九类时间线 DTO 与升序排序、异常工单 DTO、应收/应付未结金额、库存绝对移动量、失败操作和未关闭异常工单汇总；门面继续负责关键词归一化、十类租户化检索与已知业务编号扩散，并从 768 行降至 364 行，`trace` 保持只读事务；3 个独立组装测试覆盖全类型字段映射、URL 编码、null 时间排序、金额/数量精度、关闭状态与异常计数，另补 2 个依赖方向/事务结构测试；业务追溯及报表联合回归 29 项通过，临时 H2（关闭 Flyway）控制器契约 2 项通过，后端编译与 `git diff --check` 通过 |
| 2026-08-13 | E-1 MRP 需求计算/BOM 展开 Service 拆分 | 新增 `MrpPlanCalculationService`，从 `MrpPlanService.run()` 抽离销售未发货与安全库存需求收集、现存/在途采购/在制生产供给抵扣、净需求计算和 BOM 材料展开；计算协作者只读、带公司/账套参数且不依赖门面，不生成单号、不写 MRP 运行头/行、不参与转采购/生产订单；门面从 563 行进一步降至 346 行；3 个计算测试覆盖需求合并、供给覆盖短路、BOM 材料需求与租户参数，MRP/费用/库存预警/事务/原生 SQL 联合回归 54 项通过 |
| 2026-08-13 | E-1 报表库存读侧 Service 拆分 | 新增 `InventoryReportQueryService`，从 `ReportQueryService` 抽离库存余额/库存流水筛选、分页归一化、公司/账套与数据范围条件、数量可用量/流水 DTO 映射、导出行数限制和批量流式导出；门面保留原有六个库存报表公开方法并只做委托，订单与财务结算报表接口不变；4 个独立测试覆盖租户/数据范围、筛选归一化、数量映射、导出限制与批次，另补依赖方向和只读事务门禁；相关纯单元/结构/既有契约 23 项通过，控制器权限与参数绑定用临时 H2（关闭 Flyway）5 项通过；依赖真实数据的报表集成用例因本机 MySQL `root` 凭据不可用未执行，H2 又不兼容 V77 的 MySQL `DROP CHECK` 迁移语法 |
| 2026-08-13 | E-1 报表订单/财务结算读侧 Service 拆分 | 新增 `OrderReportQueryService` 与 `FinanceSettlementReportQueryService`，从 `ReportQueryService` 抽离采购/销售订单筛选、SELF/DEPT/POST 数据范围、公司账套守卫、订单 DTO 映射、应收/应付方向分流、混合方向合并分页、财务范围 SQL、水合与流式导出；`ReportQueryService` 从 639 行降至 114 行，保留原 15 个公开接口兼容委托；新增订单 4 项、财务结算 6 项独立测试，并更新依赖方向与只读事务门禁；报表相关纯单元/租户/结构/既有契约回归 40 项通过，控制器权限与参数绑定临时 H2（关闭 Flyway）5 项通过，真实 MySQL 集成用例仍受本机凭据与 H2 不兼容 MySQL V77 迁移限制 |
| 2026-08-13 | E-1 MRP 历史查询/建议行水合 Service 拆分 | 新增 `MrpPlanQueryService`，从 `MrpPlanService` 抽离历史运行状态筛选与分页归一化、计划和建议行公司/账套守卫、租户化建议行加载、采购/生产建议分组、商品编码名称 SQL 水合及 DTO 映射；主 Service 从 688 行降至 563 行，销售缺口与安全库存需求收集、现存/在途/在制供给抵扣、BOM 展开、运行结果持久化以及建议行转采购/生产订单继续留在门面，`run` 与 `convertLine` 通过查询 Service 复用详情映射和单据守卫；6 个查询测试覆盖状态归一化、分页默认值与 200 上限、全字段租户条件、建议行分组、商品水合 SQL、展示缺失兼容及跨账套/错误运行拒绝，另补依赖方向、只读/写事务和迁移门禁；MRP 专项 16 项、费用/库存预警/MRP/原生 SQL 租户扫描联合回归 51 项通过 |
| 2026-08-13 | E-1 库存预警查询/处置状态水合 Service 拆分 | 新增 `InventoryAlertQueryService`，从 `InventoryAlertService` 抽离规则筛选、公司账套隔离、库存余额批量查询、仓库/商品批量展示水合、处置状态加载、缺口加剧时自动恢复 `ACTIVE`、已恢复库存过滤及规则/预警 DTO 映射；主 Service 从 495 行降至 292 行，规则创建/编辑/启停、忽略/已处理和重新激活继续留在门面，工作台和异常规则扫描仍通过门面兼容原接口及显式审计身份重载；5 个独立查询测试覆盖规则全筛选、空结果短路、余额批量租户条件、处置状态叠加、缺口恶化恢复、库存恢复过滤和跨账套展示丢弃，既有 8 个租户/写流程测试与依赖方向、只读/写事务门禁继续通过；库存预警专项 21 项、调用方及 E-1 联合回归 161 项通过 |
| 2026-08-13 | E-1 费用过账/红冲 Service 拆分 | 新增 `ExpensePostingService`，从 `ExpenseService` 抽离附件强制留痕、会计期间、费用/支付科目校验、原凭证按来源幂等创建、借贷双分录幂等写入、费用头乐观锁更新，以及红冲凭证和借贷互换分录幂等写入；主 Service 从查询拆分后的 433 行进一步降至 242 行，创建/编辑、提交/审批/驳回、作废等状态机继续留在门面，门面和 Posting Service 的 `post`/`reverse` 均保持 REQUIRED 写事务；5 个独立过账测试覆盖新建凭证与写入顺序、原凭证/分录双层幂等、已过账短路、红冲字段与借贷互换、缺原分录零写入，另补依赖方向和事务结构门禁；费用专项 24 项、既有 E-1 联合回归 122 项通过 |
| 2026-08-13 | E-1 费用查询/凭证对账 Service 拆分 | 新增 `ExpenseQueryService`，从 `ExpenseService` 抽离状态/日期筛选与分页归一化、公司账套守卫、费用详情、原凭证/红冲凭证及租户化分录加载、借贷与费用金额核对、对账详情及 DTO 映射；主 Service 从 628 行降至 433 行，创建/编辑、提交/审批/驳回、过账、红冲、作废和凭证/分录写入继续留在门面，写流程通过查询 Service 复用费用守卫、凭证查询和响应映射；5 个独立查询测试覆盖全筛选与分页上限、空页短路、原凭证和红冲凭证完整水合、借贷及金额核对、对账 DTO 映射与跨账套拒绝，既有 5 个费用租户边界测试继续通过，另补依赖方向和只读/写事务结构门禁，专项联合回归 19 项通过 |
| 2026-08-13 | E-1 补货建议查询/展示水合 Service 拆分 | 新增 `InventoryReplenishmentSuggestionQueryService`，从 `InventoryReplenishmentSuggestionService` 抽离建议编号/状态/仓库/商品/供应商/创建时间筛选、分页归一化、公司账套隔离、仓库/商品/供应商/采购单批量展示水合、单条建议守卫、履约状态及 DTO 映射；主 Service 从 644 行降至 365 行，低库存建单、编辑、取消及转采购订单继续留在门面，写流程通过查询 Service 复用租户化建议守卫和响应映射，门面 `list` 保留空查询默认对象后委托；5 个独立查询测试覆盖全筛选与分页上限、空页短路、展示水合、跨账套展示数据丢弃、单条守卫及全部履约状态，另补门面委托、依赖方向和只读/写事务结构门禁，专项联合回归 24 项通过 |
| 2026-08-13 | E-1 手工凭证查询/分录水合 Service 拆分 | 新增 `ManualVoucherQueryService`，从 `ManualVoucherService` 抽离凭证号/状态/日期列表筛选、公司账套守卫、租户化分录加载及完整生命周期 DTO 映射；主 Service 从 642 行降至 572 行，新增/编辑、提交/审批/驳回、过账、红冲作废和草稿删除继续留在门面，写流程通过查询 Service 复用凭证守卫、分录加载与响应映射，列表仍保持原有“每行带完整分录”的接口行为且不改变既有分页上限语义；4 个独立查询测试覆盖全筛选及分页透传、空页短路、完整分录和生命周期映射、分录租户条件与跨账套拒绝，另补依赖方向和只读/写事务结构门禁，联合回归 30 项通过 |
| 2026-08-13 | E-1 异常工单查询/事件水合 Service 拆分 | 新增 `ExceptionTicketQueryService`，从 `ExceptionTicketService` 抽离列表关键字/状态/优先级/分类/负责人/来源单号/仅超时全筛选、分页归一化、公司账套隔离、事件批量水合、业务追踪 URL 编码与完整 DTO 映射；主 Service 从 637 行降至 496 行，编号生成、创建/分派/开始/解决/关闭状态机、通知和 SLA 超时升级继续留在门面，写流程通过查询 Service 复用租户化工单守卫与映射；5 个独立查询测试覆盖全筛选及分页上限、空页短路、事件租户条件、租户化单条守卫、URL 编码与空来源不可追踪，另补依赖方向和只读/写事务结构门禁；单元/结构 22 项通过，控制器权限与参数绑定 5 项用一次性 H2 覆盖通过（未修改项目测试配置） |
| 2026-08-13 | E-1 质量检验查询/账套隔离/导出 Service 拆分 | 新增 `QcInspectionQueryService`，从 `QcInspectionService` 抽离列表全字段筛选与分页归一化、公司/账套隔离、租户化明细加载、CSV 流式导出身份传递及汇总/详情 DTO 映射；主 Service 从 599 行降至 401 行，建单/编辑/提交/IQC-OQC-IPQC 判定/作废等写编排及采购入库合格数量回写继续留在门面，门面 `list`/`exportInspections` 保留空查询守卫后委托查询 Service；5 个独立查询测试覆盖全筛选归一化与分页上限、空查询默认分页、详情完整映射、明细租户条件、跨账套拒绝和 CSV 身份恢复，另补门面委托、依赖方向与只读事务结构门禁；质量域及采购入库/销售出库质检闸门联合回归 62 项通过，生产订单上下文 11 项因本机 MySQL `root` 认证失败无法启动（0 断言失败，环境阻塞不计通过） |
| 2026-08-13 | E-1 销售订单查询/数据权限 Service 拆分 | 新增 `SalesOrderQueryService`，从 `SalesOrderService` 抽离列表筛选与分页归一化、SELF/DEPT/POST 共享数据范围、订单可见性、租户化明细加载及汇总/详情 DTO 映射；主 Service 从 670 行降至 468 行，构造依赖由 17 个降至 12 个并移除零业务引用的 `ProductMapper`，读侧依赖（`CurrentUserContext`/`DataScopeService`/`ScopedUserResolver`/`UserMapper`）全部迁入查询 Service；拆分仅抽读侧，创建/编辑/提交/审核/反审核/取消等写编排继续留在门面，门面 `list` 保留 `query == null ? new SalesOrderPageQuery() : query` 空值守卫后委托查询 Service，门面与查询 Service 均保持 REQUIRED 写事务和只读读事务；6 个独立查询测试覆盖三类数据范围、全筛选归一化、分页默认值与上限、汇总/详情完整映射（含辅单位/换算率/已出库量等 13 字段）、明细租户条件、缺失单据短路及创建人组织授权，另补门面委托、依赖方向与只读/写事务结构门禁，联合回归 27 项通过 |
| 2026-08-13 | 主数据批量接线（客户/仓库/供应商） | 客户、仓库、供应商三页此前的 `<el-table-column type="selection">` 为孤儿列（渲染但未接 `@selection-change`、无批量动作），仅商品页接全。按商品页的行内 `runBatch` 模式补齐三处：展示 composable 增 `joinNames`/`labelWithCount`，列表 composable 增 `selectedRows`/`batchRunning`/`handleSelectionChange`/`runBatch`（顺序执行 + 注入 confirm/onSuccess/onWarning + 失败按 `name‖code‖id` 归集）/`handleBatchEnable`(enableXxx)/`handleBatchDisable`(deleteXxx)/`exportSelectedRowsToCsv` + `handleExportSelected`，视图补 `@selection-change` 与 `#toolbar-left` 槽（新建 + 批量启用 + 批量停用 + 导出选中，带 `v-permission` 与 `labelWithCount` 计数），中英 i18n 键齐套；三页各 4 个 composable 测试（含批量启用断言），composable 全量 696 测试通过，我引入的测试类型缺失键（`batchEnable`/`batchDisable`/`exportSelected`）已补齐；既有 DisplayPreferences / ElMessageBox.confirm 重载等类型错为基线既有，非本项引入 |
| 2026-08-12 | E-1 审批工作流查询 Service 拆分 | 新增 `WorkflowQueryService`，从 `WorkflowService` 抽离待办列表筛选与分页归一化、审批实例作用域子查询、超时待办过滤、待办/审批记录详情、审批进度 DTO 映射及共享写守卫 `requireScopedTask`；主 Service 从 752 行降至 574 行，构造依赖由 9 个增至 10 个并注入 `WorkflowQueryService`；审批工作流拆分仅抽读侧，提交/转签/升级/撤回/驳回/完成等写编排继续留在门面，门面与协作者均保持 REQUIRED 写事务和只读读事务；3 个独立查询测试覆盖空查询默认分页与实例作用域 SQL、空记录查询默认分页、仅超时待办筛选，另补门面委托、依赖方向与只读/写事务结构门禁，联合回归 30 项（含 6 项租户隔离、6 项控制器、10 项通知集成、1 项配置控制器）通过 |
| 2026-07-31 | E-1 采购收货过账 Service 拆分 | 新增 `PurchaseReceiptPostingService`，从 `PurchaseReceiptService` 抽离会计期间、入库单/采购订单权限与状态、仓库租户边界、租户化明细加载、商品批量及批次控制校验、累计剩余可入库量预检、IQC 闸门、头与订单行乐观锁、库存入库与序列号、订单收货状态刷新及财务过账完整编排；全部行在首次写入前解析为过账上下文，第二行订单行缺失、累计超限、批号缺失、商品校验或 IQC 失败均保持零写调用；主 Service 从 438 行降至 304 行，构造依赖由 15 个降至 8 个，门面与协作者均保持 REQUIRED 写事务；7 个独立过账测试覆盖双行完整字段与写入顺序、全部预检失败、头乐观锁下游隔离及明细租户条件，相关联合回归 51 项、排除既有 MySQL 认证阻塞后的采购域回归 125 项通过 |
| 2026-07-31 | E-1 采购收货查询/数据权限 Service 拆分 | 新增 `PurchaseReceiptQueryService`，从 `PurchaseReceiptService` 抽离列表全字段筛选与分页归一化、SELF/DEPT/POST 共享数据范围、CSV 流式导出身份传递、入库单/来源采购订单可见性、租户化明细加载及汇总/详情 DTO 映射；主 Service 从 710 行降至 438 行，构造依赖由 19 个降至 15 个并移除零业务引用的 `PurchaseOrderMapper`；10 个独立查询场景覆盖三类数据范围、全筛选归一化、分页默认值与上限、汇总/详情完整映射、明细租户值、缺失单据短路、两类创建人组织授权及 CSV 内容/流式线程身份恢复，另补三个门面委托与依赖方向/只读事务结构门禁，联合回归 62 项通过 |
| 2026-07-31 | E-1 采购退货过账 Service 拆分 | 新增 `PurchaseReturnPostingService`，从 `PurchaseReturnService` 抽离会计期间、退货/入库/订单权限与租户化明细加载、累计可退量与可用库存预检、退货/入库/订单明细乐观锁、库存出库与序列号、订单收货状态刷新及财务过账完整编排；全部行在首次写入前解析为过账上下文，第二行缺失入库/订单明细或累计数量超限均保持零写调用；主 Service 从 441 行降至 301 行，构造依赖由 15 个降至 9 个，门面与协作者均保持 REQUIRED 写事务；6 个独立过账测试覆盖双行完整字段与写入顺序、两类累计超限、两类来源缺失及头乐观锁下游隔离，联合回归 44 项通过 |
| 2026-07-31 | E-1 采购退货查询/数据权限 Service 拆分 | 新增 `PurchaseReturnQueryService`，从 `PurchaseReturnService` 抽离列表筛选与分页归一化、共享数据范围、CSV 流式导出身份传递、退货单/来源入库单/来源采购订单可见性、租户化明细与来源入库行加载、商品/入库单/订单/仓库上下文水合及汇总/详情 DTO 映射；主 Service 从 746 行降至 441 行，构造依赖由 21 个降至 15 个并移除零业务引用的 `ProductMapper`；12 个独立查询场景覆盖 SELF/DEPT/POST 范围、全筛选归一化、分页默认值与上限、汇总/详情完整映射、两层明细租户边界、跨账套来源拒绝、三类创建人组织授权及流式线程身份恢复，另补三个门面委托与依赖方向/只读事务结构门禁，联合回归 49 项通过 |
| 2026-07-31 | E-1 异常规则扫描 Service 拆分 | 新增 `ExceptionRuleScanService`，从 `ExceptionRuleService` 抽离低库存、应收逾期、应付逾期、失败操作日志四类发现器，以及活动工单去重、工单创建、命中新增/更新、成功失败状态回写、到期规则加载与按规则构造系统审计；主 Service 从 751 行降至 376 行，构造依赖由 11 个降至 4 个，门面与协作者六个扫描入口均保持 REQUIRED 写事务；4 个独立扫描测试覆盖应付租户条件与工单映射、失败日志时间窗口与字段映射、依赖异常失败回写及按规则租户/账套传递调度审计，另补门面委托和依赖方向/事务结构门禁，联合回归 31 项通过 |
| 2026-07-31 | E-1 销售退货过账 Service 拆分 | 新增 `SalesReturnPostingService`，从 `SalesReturnService` 抽离会计期间、退货/发货/订单权限与租户化明细加载、累计数量和批次预检、原出库实际成本冲回、退货/发货/订单乐观锁、库存入库与序列号、订单交付状态刷新及财务过账完整编排；所有行的数量、批次与成本在首次写入前预计算，第二行失败保持零写调用；主 Service 从 616 行降至 319 行，构造依赖由 15 个降至 9 个，门面与协作者均保持 REQUIRED 写事务；6 个独立测试覆盖双行实际成本汇总、显式/自动批次日期库位与序列号透传、缺成本/多批次/两类累计超限零写入及头乐观锁下游隔离，联合回归 47 项通过 |
| 2026-07-31 | E-1 销售退货查询/数据权限 Service 拆分 | 新增 `SalesReturnQueryService`，从 `SalesReturnService` 抽离列表筛选与分页归一化、共享数据范围、退货单/源发货单/源销售订单可见性、租户化明细加载及汇总/详情 DTO 映射；主 Service 从 810 行降至 616 行，构造依赖由 19 个降至 15 个并移除零业务引用的 `ProductMapper`；10 个查询场景覆盖 SELF/DEPT/POST 范围、全筛选归一化、分页默认值与上限、汇总/详情完整映射、明细租户边界及三类创建人组织授权，另补依赖方向、只读事务与共享范围扫描门禁，联合回归 34 项通过 |
| 2026-07-31 | E-1 财务总账凭证过账 Service 拆分 | 新增 `FinanceVoucherPostingService`，从 `FinancePostingService` 抽离凭证头来源幂等、租户化科目解析与默认科目并发补齐、采购含税分录、销售收入/退回及成本追加、生产双边分录和库存调整分录；主 Service 从 585 行降至 262 行，构造依赖由 4 个降至 2 个，9 个门面入口及协作者均保持 REQUIRED 写事务和先子账后总账顺序；9 个独立测试覆盖采购含税三分录、销售成本追加/幂等、空调整短路、四类生产科目映射及非正金额短路，另补依赖方向/事务结构与租户回归，联合回归 13 项通过 |
| 2026-07-31 | E-1 财务应收应付子账过账 Service 拆分 | 新增 `FinanceSubledgerPostingService`，从 `FinancePostingService` 抽离应收/应付来源幂等、客户/供应商账期、租户边界及审计字段持久化；主 Service 从 741 行降至 585 行，构造依赖由 7 个降至 4 个，门面与协作者均保持 REQUIRED 写事务；3 个独立行为测试覆盖同租户账期、跨租户主数据回退及重复来源零写入，另补依赖方向/事务结构与原有租户回归，联合回归 6 项通过 |
| 2026-07-29 | E-1 销售发货过账 Service 拆分 | 新增 `SalesDeliveryPostingService`，从 `SalesDeliveryService` 抽离会计期间、订单/库存/预占累计量、OQC 门禁、发货与订单乐观锁、预占释放、库存出库/序列号、订单交付状态刷新及实际库存成本结转财务的完整编排；主 Service 从 688 行降至 512 行，构造依赖由 15 个降至 11 个，门面与协作者均保留 REQUIRED 写事务；4 个独立测试覆盖双行实际成本汇总、空批次 FEFO 触发契约、显式批次/日期/库位及序列号透传、库存不足/预占不足/QC 拒绝零写入短路，另补门面委托与禁止过账依赖回吸门禁，联合回归 41 项通过 |
| 2026-07-29 | 采购询价落败报价乐观锁门禁 | 选定中标报价时，任一落败 `PENDING` 报价更新为 `REJECTED` 若发生乐观锁冲突，立即抛出业务冲突并阻止询价单进入 `CLOSED`，由既有外层事务统一回滚中标状态；新增落败报价更新返回 0 的回归，断言询价单状态与中标引用均不更新 |
| 2026-07-29 | E-1 采购询价报价 Service 拆分 | 新增 `PurchaseInquiryQuoteService`，从 `PurchaseInquiryService` 抽离供应商有效性、报价防重与逐行价格校验、报价头/行持久化、中标/落败状态流转、租户化报价读取及 DTO 映射；主 Service 从 913 行降至 547 行，构造依赖由 9 个降至 7 个，询价单状态与乐观锁继续留在单一外层事务；20 个服务回归覆盖原有录价/选标/预填/转单并新增落败报价拒绝态、供应商与报价租户边界、报价乐观锁失败，另补依赖方向与事务边界结构门禁，联合回归 31 项通过 |
| 2026-07-29 | IPQC 判定分支修复 | IPQC 从误走 IQC 采购入库回写改为与 OQC 共用 gate-only 判定内核，仅更新质检头/行并汇总合格量；判定前通过 `QcInspectionSourceAccess` 重新校验生产工单租户、账簿、删除标志及 RELEASED/MATERIAL_ISSUED 状态，建单同步复用且 `QcInspectionCreateService` 不再直依赖生产工单 Mapper；6 个判定回归覆盖成功、不更新来源、三种非法状态、跨租户与已删除，3 个生产完工闸门回归覆盖未判定/合格不足阻断及足量放行 |
| 2026-07-29 | E-1 质量检验建单 Service 拆分 | 新增 `QcInspectionCreateService` 与中性 `QcInspectionSourceAccess`，从 `QcInspectionService` 抽离 IQC/OQC/IPQC 三类建单、来源单据租户/草稿校验、租户化明细读取及进行中检验防重；主 Service 从 861 行降至 584 行，构造依赖由 9 个降至 7 个，判定侧复用统一来源访问且不反向依赖建单语义；11 个独立测试覆盖三类建单、跨租户与状态拒绝、采购/发货明细范围、三类进行中门禁和写事务声明，联合质检与结构回归 30 项通过 |
| 2026-07-29 | E-1 销售发货查询/数据权限 Service 拆分 | 新增 `SalesDeliveryQueryService`，从 `SalesDeliveryService` 抽离列表筛选与分页归一化、共享数据范围、发货单及源销售订单可见性、租户化明细加载和汇总/详情 DTO 映射；主 Service 从 895 行降至 688 行，构造依赖由 19 个降至 15 个并移除零业务引用的 `ProductMapper`；9 个独立测试覆盖 SELF/DEPT/POST 范围、全筛选归一化、分页默认值与上限、汇总/详情映射、明细租户边界及两类创建人组织授权，另补门面委托与禁止权限依赖回吸的结构门禁 |
| 2026-07-29 | E-1 采购订单查询/导出 Service 拆分 | 新增 `PurchaseOrderQueryService`，从 `PurchaseOrderService` 抽离列表分页与筛选归一化、共享数据范围、详情可见性、汇总 DTO 映射和异步 CSV 导出身份传递；主 Service 从 788 行降至 564 行，构造依赖由 14 个降至 11 个；6 个独立测试覆盖 SELF/DEPT/POST 范围、分页上限、供应商水合、创建人组织授权、CSV 行映射与流式线程身份恢复，另补门面委托及禁止依赖回吸的结构门禁 |
| 2026-07-29 | E-1 采购订单追踪 Service 拆分 | 新增 `PurchaseOrderTraceService`，从 986 行 `PurchaseOrderService` 抽离收货→退货→应付→付款→凭证全链路加载、执行数量汇总与追踪 DTO 映射；主 Service 降至 788 行，构造依赖由 19 个降至 14 个；2 个独立追踪测试覆盖完整链路、付款去重与空链路短路，另补外层授权后委托测试，并把新 Service 纳入只读事务结构门禁 |
| 2026-07-29 | E-1 往来对账单页拆分 | 新增 `useFinanceStatementPresentation`、`useFinanceStatementQuery`，从往来对账单页抽离金额日期/往来与单据方向标签/打印 DTO、客户供应商选项与对账查询；筛选变化同步清空旧结果并用请求序号阻止慢响应回填，反向日期拒绝请求，打印异常准确反馈，Long ID 保持字符串；15 个独立测试，国际化与整块契约同步覆盖 |
| 2026-07-29 | E-1 在线用户会话页拆分 | 新增 `useSystemUserSessionPresentation`、`useSystemUserSessionList`，从在线会话页抽离状态/姓名/时间展示、查询分页与单会话/整用户撤销；改页大小会复位首页，确认取消静默、异常与 API 失败准确反馈，超安全整数 Long ID 原样传递；13 个独立测试，国际化与整块契约同步覆盖页面及两个 composable |
| 2026-07-29 | E-1 仓库库位管理页拆分 | 新增 `useWarehouseLocationPresentation`、`useWarehouseLocationList`、`useWarehouseLocationForm`，从库位管理页抽离仓库/状态/默认库位展示、仓库选项与查询分页/启停、新建编辑表单；补上原页面缺失的真实分页、重置入口和加载/保存/启停失败双语反馈，17 个独立测试及国际化/整块契约门禁 |
| 2026-07-29 | E-1 凭证查询页拆分 | 新增 `useVoucherPresentation` 与 `useVoucherList`，从凭证查询页抽离来源/状态/金额日期展示、筛选分页、详情并行加载与打印数据组装；分页改为独立 page/size 处理器并配套独立测试，契约门禁同步按页面与两个 composable 整块校验 |
| 2026-07-28 | E-1 通知中心页拆分 | 新增 `useSystemNotificationPresentation` 与 `useSystemNotificationList`，从通知中心页抽离读状态展示、查询分页/未读计数、查看即已读及单条/批量/全部已读流程，并配套独立测试 |
| 2026-07-28 | E-1 应收应付共享页拆分 | 新增 `useFinanceAccountPresentation`、`useFinanceAccountList`、`useFinanceAccountResources`，从应收/应付共享页抽离本地化展示、权限门控的双实例查询/导出/详情和客户/供应商选项加载，并配套独立测试；页面保留默认页签、路由关键字和生命周期编排 |
| 2026-07-28 | E-1 附件中心页拆分 | 新增 `useAttachmentPresentation`、`useAttachmentList`、`useAttachmentUploadForm`，从附件中心页抽离文件大小展示、查询分页/下载/删除和上传对话框/文件/校验提交流程，并配套独立测试；契约门禁同步按页面与三个 composable 整块校验 |
| 2026-07-28 | 采购订单 CSV 数据范围修复 | 列表与导出共用 `scopedListQuery`，统一查询规范化并强制 SELF/DEPT/POST 数据范围；参数化回归覆盖三种范围，data-scope smoke 新增 S6c CSV 验证 |
| 2026-07-17 | 2.1 打印 | fe `647dbfb` |
| 2026-07-17 | 2.7 对账单 | 本提交 V114 + statement 模块 |
| 2026-07-17 | 2.10 毛利 | margin 模块 |
| 2026-07-22 | 2.11 客户信用概览 | 信用敞口 API + 客户详情额度占用 |
| 2026-07-22 | 2.12 商品库存概览 | 跨仓库存汇总 API + 商品详情展示 |
| 2026-07-22 | 2.13 供应商应付敞口概览 | 应付与采购承诺汇总 API + 供应商详情展示 |
| 2026-07-22 | 2.14 仓库库存概览 | 仓库库存汇总 API + 仓库详情展示 |
| 2026-07-22 | 2.15 销售订单授信预警 | 提交前额度校验 + `/api/sales/orders/credit-preview` + 销售订单页授信预览 |
| 2026-07-17 | 2.2 IPQC | V115 + 完工闸门 |
| 2026-07-17 | 2.4 发票关联 | relatedBiz 校验 PO/SO |
| 2026-07-17 | 2.5 核销 UX | 收付款多选分摊 |
| 2026-07-17 | 2.9 销售报价 | V116 + 转 SO |
| 2026-07-17 | 2.3 MRP | V117 + run 接口 |
| 2026-07-17 | 2.6 转签 | transfer API |
| 2026-07-22 | 2.6 审批时限配置 | V122 + 审批配置页 + 新任务/升级截止时间 |
| 2026-07-22 | 2.6 超时待办筛选 | `/api/workflow/tasks?overdueOnly=true` + 审批待办筛选项 |
| 2026-07-17 | 2.8 批次过账 | 入库强制批号；出库支持显式批号或 FEFO/FIFO 自动拣选 |
| 2026-08-19 | 2.8 批次端到端谱系 | 新增双向批次谱系 API/页面，跨生产工单追踪供应商与客户，支持召回清单导出、节点/深度/循环/数据范围保护；V145 菜单权限种子及迁移回归测试 |
| 2026-07-17 | P2-1 看板账龄 | dashboard 嵌入 |
| 2026-07-22 | P2-1 审批超时预警 | 工作台超时计数 + 超时待办入口 |
| 2026-07-22 | P2-1 TOP SKU | 最近 30 天已过账发货按商品汇总 TOP 5 |
| 2026-07-22 | P2-8 客户/供应商主数据双语补齐 | `PageForm` / `PageTable` / `SearchBar` / `StatusTag` 双语 + customers/suppliers 页面文案与货币/时间本地化 + 客户/供应商字段契约补齐与主数据停用态统一 |
| 2026-07-22 | P2-8 产品/仓库主数据双语补齐 | `TableColumnSetting` 双语 + products/warehouses 页面文案、批量操作、导出文件名与金额/时间本地化 |
| 2026-07-23 | P2-8 销售订单双语补齐 | 销售订单查询、列表、编辑、授信预览、状态、校验、确认框及操作反馈完整双语化 + i18n 回归测试 |
| 2026-07-23 | P2-8 销售发货双语补齐 | 销售发货查询、列表、编辑、扫码计数、状态、校验与操作反馈完整双语化；创建时间接入用户时区 + i18n 回归测试 |
| 2026-07-23 | P2-8 采购订单双语补齐 | 采购订单查询、编辑、详情、执行追踪、审批/反审核/关闭、导出与反馈完整双语化；创建时间接入用户时区 + i18n 回归测试 |
| 2026-07-23 | P2-8 采购收货双语补齐 | 采购收货查询、编辑、详情、关联订单、扫码计数、过账/取消、导出与反馈完整双语化；创建时间接入用户时区 + i18n 回归测试 |
| 2026-07-23 | P2-8 采购退货双语补齐 | 采购退货查询、编辑、详情、关联收货单、过账库存/应付冲减、取消、导出与反馈完整双语化；创建时间接入用户时区 + i18n 回归测试 |
| 2026-07-23 | P2-8 生产订单双语补齐 | 生产订单查询、编辑、详情、下达/领料、工序报工、完工/红冲、退料、状态校验与操作反馈完整双语化；创建时间接入用户时区 + i18n 回归测试 |
| 2026-07-23 | P2-8 生产 BOM 双语补齐 | BOM 查询、列表、物料明细编辑、详情、状态、校验与操作反馈完整双语化；列表及详情创建时间接入用户时区 + i18n 回归测试 |
| 2026-07-23 | P2-8 生产工作中心双语补齐 | 工作中心查询、列表、新增/编辑、启停确认、状态、校验与操作反馈完整双语化；补齐加载失败用户提示 + i18n 回归测试 |
| 2026-07-23 | P2-8 生产工艺路线双语补齐 | 工艺路线查询、列表、BOM 绑定、工序明细编辑、详情、启停确认、逐行校验与反馈完整双语化；补齐选项/列表/详情加载失败提示 + i18n 回归测试 |
| 2026-07-23 | P2-8 质量检验双语补齐 | IQC/IPQC/OQC 查询、建单、提交、判定、作废、导出、状态与逐行校验完整双语化 + i18n 回归测试 |
| 2026-07-23 | P2-8 MRP/补货建议双语补齐 | MRP 运行参数、建议结果及补货建议查询、编辑、转采购订单、状态/校验/反馈完整双语化 + `inventory.test.ts` 中英文键回归 |
| 2026-07-23 | P2-8 应收/应付账款双语补齐 | 两个重复页面抽为共享组件；筛选、列表、详情、反馈与导出文件名完整双语化，金额/时间本地化并保留细粒度 tab 与主数据选项权限回退 |
| 2026-07-23 | P2-8 业务时区日期修复 | 新增 `formatBusinessDate` / `getBusinessMonthDateRange`，替换页面中 UTC `toISOString()` 日期截断，覆盖跨时区日界与月初范围测试 |
| 2026-07-23 | 工程门禁真实接入 | workflow 移至仓库根 `.github/workflows`，后端使用 JDK 17 + MySQL 8.4 service，前端独立执行测试/类型/Lint/契约/构建；根级 YAML 与定向配置测试通过 |
| 2026-07-26 | E-1 生产订单工序报工拆分 | 新增 `useProductionOrderOperations`，从生产订单页抽离工序加载/报工对话框状态与提交流程，并配套独立测试 |
| 2026-07-26 | E-1 生产订单完工/红冲拆分 | 新增 `useProductionOrderCompletion`，从生产订单页抽离完工与红冲对话框状态、控制标志校验与提交流程，并配套独立测试 |
| 2026-07-26 | E-1 生产订单领料/退料拆分 | 新增 `useProductionOrderMaterials`，从生产订单页抽离领料/退料对话框状态、控制标志校验与提交流程，并配套独立测试 |
| 2026-07-26 | E-1 生产订单表单/下达/取消拆分 | 新增 `useProductionOrderForm`，从生产订单页抽离新增编辑表单、产品-BOM 联动、下达与取消流程，并配套独立测试 |
| 2026-07-26 | E-1 生产订单列表/选项/查看打印拆分 | 新增 `useProductionOrderList`，从生产订单页抽离查询分页、主数据选项、库位加载与查看/打印流程，并配套独立测试 |
| 2026-07-26 | E-1 采购收货展示拆分 | 新增 `usePurchaseReceiptPresentation`，从采购收货页抽离金额/库位展示、仓库库位过滤与草稿/已完成计数，并配套独立测试 |
| 2026-07-26 | 采购收货供应商筛选修复 | 收货列表供应商下拉补齐真实供应商选项加载；修复页头样式类名与模板不一致导致的样式失效 |
| 2026-07-26 | E-1 采购收货列表/导出/过账拆分 | 新增 `usePurchaseReceiptList`，从采购收货页抽离查询分页、导出、查看/打印、过账/取消、关联订单与主数据选项加载，并配套独立测试 |
| 2026-07-26 | E-1 采购收货表单/扫码拆分 | 新增 `usePurchaseReceiptForm`，从采购收货页抽离新增编辑表单、订单联动、扫码计数、控制标志校验与提交流程，并配套独立测试 |
| 2026-07-26 | E-1 采购订单展示拆分 | 新增 `usePurchaseOrderPresentation`，从采购订单页抽离金额格式化、待审/已审计数与取消/关闭/反审核可操作判定，并配套独立测试 |
| 2026-07-26 | E-1 采购订单列表/审批动作拆分 | 新增 `usePurchaseOrderList`，从采购订单页抽离查询分页、导出、打印、提交/审核/反审核/驳回/取消/关闭/追踪与主数据选项加载，并配套独立测试 |
| 2026-07-26 | E-1 采购订单表单/明细价格拆分 | 新增 `usePurchaseOrderForm`，从采购订单页抽离新增编辑复制、明细增删、辅单位换算、采购价解析与提交流程，并配套独立测试 |
| 2026-07-26 | E-1 商品管理展示/列表拆分 | 新增 `useProductPresentation` 与 `useProductList`，从商品管理页抽离格式化展示、查询分页、批量启停、导出与详情库存概览，并配套独立测试 |
| 2026-07-26 | E-1 客户管理展示/列表拆分 | 新增 `useCustomerPresentation` 与 `useCustomerList`，从客户管理页抽离额度展示格式化、查询分页、启停/删除/导出与信用敞口详情加载，并配套独立测试 |
| 2026-07-26 | E-1 供应商管理展示/列表拆分 | 新增 `useSupplierPresentation` 与 `useSupplierList`，从供应商管理页抽离结算/账期展示、查询分页、启停/删除/导出与应付敞口详情加载，并配套独立测试 |
| 2026-07-26 | E-1 仓库管理展示/列表拆分 | 新增 `useWarehousePresentation` 与 `useWarehouseList`，从仓库管理页抽离部门/管理员标签展示、查询分页、选项加载、启停/删除/导出与库存概览详情，并配套独立测试 |
| 2026-07-26 | E-1 财务期间展示/月结动作拆分 | 新增 `useFinancePeriodPresentation` 与 `useFinancePeriodActions`，从财务期间页抽离状态/差异标签展示与生成/检查/锁定/结账/反结账/对账向导流程，并配套独立测试 |
| 2026-07-26 | E-1 采购退货展示/列表拆分 | 新增 `usePurchaseReturnPresentation` 与 `usePurchaseReturnList`，从采购退货页抽离草稿/完成计数、库位过滤与查询/打印/过账/取消/导出/关联收货查看流程，并修复页头样式类名 |
| 2026-07-26 | E-1 销售发货展示/列表拆分 | 新增 `useSalesDeliveryPresentation` 与 `useSalesDeliveryList`，从销售发货页抽离物流状态展示、库位过滤与查询/打印/过账/取消/物流推进/主数据选项加载流程，并配套独立测试 |
| 2026-07-26 | E-1 销售订单展示/列表拆分 | 新增 `useSalesOrderPresentation` 与 `useSalesOrderList`，从销售订单页抽离状态/审批标签展示与查询/打印/提交/审核/反审核/驳回/取消流程，并配套独立测试 |
| 2026-07-26 | E-1 销售退货展示/列表拆分 | 新增 `useSalesReturnPresentation` 与 `useSalesReturnList`，从销售退货页抽离发货单标签/库位过滤展示与查询/打印/过账/取消/主数据选项加载流程，并配套独立测试 |
| 2026-07-26 | E-1 采购退货表单拆分 | 新增 `usePurchaseReturnForm`，从采购退货页抽离新增编辑、收货单联动明细、控制标志校验与提交流程，并配套独立测试 |
| 2026-07-26 | E-1 销售退货表单拆分 | 新增 `useSalesReturnForm`，从销售退货页抽离新增编辑、发货单联动明细、控制标志校验与提交流程，并配套独立测试 |
| 2026-07-26 | E-1 销售发货表单/扫码拆分 | 新增 `useSalesDeliveryForm`，从销售发货页抽离新增编辑、订单联动、扫码计数、控制标志校验与提交流程，并配套独立测试 |
| 2026-07-26 | E-1 销售订单表单/授信预览拆分 | 新增 `useSalesOrderForm`，从销售订单页抽离新增编辑复制、明细价格解析、授信预览与提交流程，并配套独立测试 |
| 2026-07-26 | E-1 采购询价展示/列表拆分 | 新增 `usePurchaseInquiryPresentation` 与 `usePurchaseInquiryList`，从采购询价页抽离状态/商品/供应商标签展示与查询/提交/作废/打印/主数据选项加载流程，并配套独立测试 |
| 2026-07-26 | E-1 库存预警展示/列表拆分 | 新增 `useInventoryAlertPresentation` 与 `useInventoryAlertList`，从库存预警页抽离类型/状态展示、统计汇总与查询/规则管理/忽略处理/补货建议流程，并配套独立测试 |
| 2026-07-26 | E-1 主数据表单拆分 | 新增/接入 `useCustomerForm`、`useProductForm`、`useSupplierForm`、`useWarehouseForm`，从客户/商品/供应商/仓库管理页抽离新增编辑表单状态、校验与提交流程，并配套独立测试 |
| 2026-07-26 | E-1 库存盘点展示/列表/表单拆分 | 新增 `useInventoryCheckPresentation`、`useInventoryCheckList`、`useInventoryCheckForm`，从库存盘点页抽离仓库/商品/库位标签与状态标签展示、查询分页/打印/调整/取消与选项加载、盘点表单（仓库带出账面库存、控制标志水合、按实盘数量校验批次与序列号）；22 个独立测试，契约门禁同步纳入三个 composable |
| 2026-07-26 | E-1 库存调拨展示/列表/表单拆分 | 新增 `useInventoryTransferPresentation`、`useInventoryTransferList`、`useInventoryTransferForm`，从库存调拨页抽离仓库/商品/库位/状态标签展示与调出调入库位过滤、查询分页/打印/过账/取消与选项加载、调拨表单（同仓校验、控制标志水合、序列号数量校验）；21 个独立测试，契约门禁改为按视图+三个 composable 整块校验并把「过账」文案断言换成真实按钮与 i18n 键 |
| 2026-07-26 | E-1 库存调整展示/列表/表单拆分 | 新增 `useInventoryAdjustmentPresentation`、`useInventoryAdjustmentList`、`useInventoryAdjustmentForm`，从库存调整页抽离盈亏类型/状态标签与仓库库位展示、查询分页/打印/过账/取消/选项加载、盈亏单表单（库位按仓库过滤、控制标志水合、序列号按调整数量校验）；23 个独立测试，契约门禁改为按整块特性校验 |
| 2026-07-26 | E-1 费用管理展示/列表/表单拆分 | 新增 `useExpensePresentation`、`useExpenseList`、`useExpenseForm`，从费用管理页抽离科目树扁平化/费用与付款科目过滤/状态与对账通过判定、查询分页与提交-审批-过账-红冲-作废-取消六类动作及对账加载、新建编辑与驳回原因流程；27 个独立测试，契约门禁改为按视图+三个 composable 整块校验 |
| 2026-07-26 | E-1 质量检验页拆分 | 新增 `useQcInspectionPresentation`、`useQcInspectionList`、`useQcInspectionCreate`、`useQcInspectionEdit`，从质检页抽离状态/类型/来源单据展示、查询分页与提交/作废/导出/打印、IQC/OQC/IPQC 建单来源加载与校验、草稿编辑与判定数量校验；建单与导出日期改用 `formatBusinessDate` 业务时区，不再用浏览器本地时间；30 个独立测试
| 2026-07-26 | E-1 采购询价表单/报价拆分 | 新增 `usePurchaseInquiryForm` 与 `usePurchaseInquiryQuotes`，从采购询价页抽离新建编辑明细表单与报价录入/中标选定流程，并清除页面内重复的提交/作废/打印实现 |
| 2026-07-26 | 生产 BOM 浏览器打印 | BOM 列表补打印入口；`bizPrint` 输出产品、基准数量与物料明细 |
| 2026-07-26 | 工艺路线浏览器打印 | 工艺路线列表补打印入口；`bizPrint` 输出路线头与工序明细 |
| 2026-07-26 | 工作中心浏览器打印 | 工作中心列表补打印入口；`bizPrint` 输出编码、名称、状态与备注 |
| 2026-07-26 | 销售/采购价目浏览器打印 | 销售价目与采购价目列表补打印入口；`bizPrint` 输出适用范围与价格区间 |
| 2026-07-26 | E-1 生产订单控制标志水合拆分 | 新增 `useProductionOrderProductControls`，从生产订单页抽离 lot/serial 控制标志缓存解析与物料水合，并配套独立测试 |
| 2026-07-26 | 手工凭证浏览器打印 | 手工凭证列表补打印入口，复用凭证打印模板输出分录 |
| 2026-07-26 | E-1 生产订单展示拆分 | 新增 `useProductionOrderPresentation`，从生产订单页抽离状态/优先级/工序/仓库展示映射，并配套独立测试 |
| 2026-07-26 | 凭证/对账单浏览器打印 | 凭证查询与往来对账单补打印入口；`bizPrint` 输出分录与对账流水 |
| 2026-07-26 | 收付款浏览器打印 | 收款/付款列表补打印入口；`bizPrint` 输出核销分配明细 |
| 2026-07-26 | 费用/发票浏览器打印 | 费用单与发票登记列表补打印入口；`bizPrint` 输出费用科目与发票金额税额 |
| 2026-07-26 | 质检/询价浏览器打印 | 质检单与采购询价列表补打印入口；`bizPrint` 输出检验行与询价明细 |
| 2026-07-26 | 库存盘点浏览器打印 | 盘点单列表补打印入口；`bizPrint` 输出账面/实盘/差异明细 |
| 2026-07-26 | 库存调整/调拨浏览器打印 | 调整单与调拨单列表补打印入口；`bizPrint` 输出单据头与明细 |
| 2026-07-26 | 请购单浏览器打印 | 请购列表补打印入口；`bizPrint` 输出请购头与明细，并补齐商品/供应商显示名 |
| 2026-07-26 | 销售报价打印 + 领料行备注 | 销售报价列表补打印；生产领料明细优先使用行备注，不再被表头备注覆盖 |
| 2026-07-26 | 退货/生产订单浏览器打印 | 销售退货、采购退货、生产订单列表补打印入口；`bizPrint` 扩展对应单据模板与明细行输出 |
| 2026-07-26 | 生产领料对话框与控制标志水合 | 领料从一键剩余需求改为可部分领料对话框；支持库位/批次/序列号进度与保存前校验；领料/退料/完工控制标志改为按商品 API 水合，不再依赖前 200 个商品选项缓存 |
| 2026-07-26 | 序列号台账双语补齐 | 序列号页状态筛选/标签双语化，商品选项字段兼容，错误提示与占位文案补齐 |
| 2026-07-26 | 销售发货物流筛选 | 列表支持物流状态/运单号筛选与双语状态标签；后端列表补运单号模糊过滤；表单查看态禁用物流字段 |
| 2026-07-26 | 盘点控制标志校验 | 库存盘点明细水合 lot/shelf/serial、序列号进度与保存前校验；按实际数量校验序列号数量 |
| 2026-07-26 | 生产完工/退料控制标志校验 | 生产订单完工与退料补齐库位加载、商品 lot/shelf/serial 标志水合、序列号进度与保存前校验；修复完工/退料库位列表未定义导致的半成品交互 |
| 2026-07-26 | E-1 库存动作拆分 | 新增 `useInventoryStockActions`，从库存查询页抽离批次/流水/效期/追溯/预留对话框开关、查询复位与预留释放/核对流程；6 个独立测试覆盖范围继承、释放成功与失败语义；契约门禁同步纳入 actions |
| 2026-07-26 | E-1 收付款展示/列表/表单/详情拆分 | 新增 `useSettlementPresentation`、`useSettlementList`、`useSettlementForm`、`useSettlementDetail`，收款与付款两个对称标签页共用同一套泛型 composable：客户/供应商名称与金额本地化、状态标签，查询/分页/打印/取消，未结单据加载与按剩余额度先旧后新自动分摊、超额校验与建单，以及共用详情对话框；32 个独立测试覆盖分摊边界（部分收款、超额、零剩余、重算）；契约门禁改为按视图+详情 composable 整块校验真实详情接口调用 |
| 2026-07-25 | 退货/调整/调拨控制标志校验 | 采购退货、销售退货、库存调整、库存调拨接入控制标志水合、序列号进度与保存前 lot/serial 校验 |
| 2026-07-25 | 收货/发货辅单位展示与序列号录入进度 | 收货/发货明细展示辅单位换算；序列号改为多行录入并显示已录/应录进度 |
| 2026-07-25 | 盘点差异调整传递序列号 | 盘点明细持久化 `serial_nos`（V144），生成调整单时透传；前端盘点行增加序列号录入 |
| 2026-07-25 | 期初库存序列号 + 批次追溯深链 | 期初导入支持 `serial_nos` 并登记入库序列号；`OPENING_BALANCE` 追溯可跳转导入页；批次追溯展示库位 |
| 2026-07-25 | 单据控制标志水合与前端校验 | `hydrateProductLineLabels` 始终补齐 lot/shelf/serial 标志；收货/发货保存前校验批次/效期/序列号数量，并按标志禁用无关字段 |
| 2026-07-25 | 仓库导入默认库位 + 库位导入 | 仓库导入提交后自动 `ensureDefaultLocation`；新增 `LOCATION` 导入类型与前端入口，支撑多库位期初库存 |
| 2026-07-25 | 商品导入条码与控制标志 | 商品导入补 `barcode` 及 `lot/shelf/inspection/serial` 控制字段；模板、校验、落库与单元测试同步 |
| 2026-07-23 | V123 主数据导入字段补齐 | 客户导入补 `customer_type/email`，供应商导入补 `email/credit_period`，模板、校验、落库、导出与租户边界测试同步 |
| 2026-07-23 | V124 关键权限种子补齐 | 补 `inventory:alert:create`、`finance:receipt:view`、`system:sequence-rule:view` 及 ERP_ADMIN 绑定，修正库存预警按钮父节点并增加迁移回归 |
| 2026-07-23 | 401 运行时登录态清理 | 静默刷新/用户信息加载失败统一清除 token、userInfo、permissions 与运行时菜单；用无循环依赖的 auth session 协调层跳转登录并覆盖并发刷新失败测试 |
| 2026-07-23 | V126 运行时菜单路由对齐 | 运行时树过滤隐藏/停用/孤儿/循环节点；修正 QC、补货与采购反审核父级，隐藏聚合后的历史伪路由并校正组件路径 |
| 2026-07-23 | 应收/应付细粒度读取权限 | 双标签页仅展示和请求获授权账款；客户/供应商筛选项同时要求对应主数据读取权限，消除单侧财务角色的正常 403 噪声 |
| 2026-07-23 | 应收/应付真实查询契约 | 后端补编号模糊查询、客户/供应商名称及创建/更新时间；前端状态收口为 `UNSETTLED/PARTIALLY_SETTLED/SETTLED/OFFSET`，直接使用 `sourceNo/bizDate/createdTime/updatedTime`，不再伪装关联订单、到期日或创建时间 |
| 2026-07-23 | V125 询价原子转采购订单 | 新增带双向结构化来源的原子转换端点；租户/账套行锁、唯一约束和同事务写入保证并发幂等，重试返回原采购订单，前端不再用预填数据二次创建 |
| 2026-07-23 | V127 询价逐行报价 | 新增租户/账套隔离的报价明细表并回填历史 header 报价；录价强制按 `inquiryLineId` 完整覆盖且拒绝缺失、重复、跨询价，选标/预填/转换均按行取价，单行旧契约保留兼容 |
| 2026-07-23 | P2-8 财务分析页双语补齐 | 账龄分析、毛利简报、往来对账单完成中英文文案及金额、数字、百分比、业务日期本地化，并增加双语键与硬编码回归测试 |
| 2026-07-23 | 国际化契约门禁同步 | 契约检查改为校验财务凭证/会计科目的 i18n 键及并行详情加载，不再依赖已移除的中文硬编码和串行赋值；`npm run check:contracts` 通过 |
| 2026-07-23 | 登录页半成品交互收口 | 移除无持久化行为的“记住密码”假控件；“忘记密码”提供管理员重置指引；本地测试账号提示完整双语化并增加页面契约测试 |
| 2026-07-23 | P2-8 多语言/时区终验 | 全量页面 template 去注释扫描无中文硬编码；国际化模块键对齐、英文值、页面引用和本地化格式测试通过，P2-8 从 DOING 收口为 DONE |
| 2026-07-23 | E-1 库存查询页首轮拆分 | 新增 `useInventoryStockPresentation`，从 1311 行库存页抽离响应式主数据映射、枚举展示/标签类型和数值格式化；3 个独立测试覆盖动态更新、未知值兼容及格式口径 |
| 2026-07-23 | E-1 库存查询页查询状态拆分 | 新增 `useInventoryStockQueries`，集中主查询、预留、批次、流水、效期预警、批次追溯六类状态及行级范围继承；3 个测试覆盖 URL 初始范围、分页复位与行级覆盖 |
| 2026-07-23 | E-1 库存详情加载拆分 | 新增 `useInventoryStockDetails`，集中库存、批次、流水、预留及预留来源五种详情状态和 API 编排；6 个测试覆盖全部成功/失败弹窗语义及错误消息键 |
| 2026-07-23 | E-1 契约门禁适配 | 库存契约门禁同时校验页面交互入口与拆出的详情 composable，继续验证真实 API 调用，不因模块拆分产生误报或降低检查强度 |
| 2026-07-17 | P2-6 单据复制 | SO/PO 复制 |
| 2026-07-17 | E-4 扩展 smoke | p2-features-api-smoke.cjs |
| 2026-07-17 | P2-3 批量已读 | read-batch API |
| 2026-07-17 | P2-4 导入错误 | 错误横幅 |
| 2026-07-17 | P2-9 引导 | 工作台提示 |
| 2026-07-17 | E-3 前端测 | p2-extensions.test.ts |
| 2026-07-17 | P2-5 附件强制 | EXPENSE 提交/过账 |
| 2026-07-17 | E-2 菜单缓存 | loadAllMenus |
| 2026-07-17 | E-5 索引 | V118 |
| 2026-07-21 | P2-7 Web 扫码录入 | V119 + `/api/masterdata/products/by-barcode` + `BarcodeScanField` + 采购收货/销售发货扫码计数；`UI_SMOKE_WORKFLOW=purchase-receipt-draft-edit,sales-delivery-draft-edit` 通过 |
| 2026-07-22 | P2-8 工作台双语补齐 | dashboard 页面硬编码清理 + 本地化货币/时间格式 + `/api/dashboard/operations` 待办文案双语化 |
| 2026-07-22 | P2-10 OpenAPI 生成类型 | `docs/openapi/product-api.json` + `openapi-typescript` + `npm run openapi:check`；产品 API 已引用生成 schema，手工契约检查移除产品字符串断言 |
| 2026-07-26 | E-1 手工凭证展示/列表/表单拆分 | 新增 `useManualVoucherPresentation`、`useManualVoucherList`、`useManualVoucherForm`，从手工凭证页抽离状态选项/标签与科目标签金额展示、查询分页/科目加载/提交审批过账删除/作废驳回/详情打印、借贷分录编辑（互斥、按分借贷平衡、可保存校验）；24 个独立测试。同时修复查询未复位分页与编辑详情加载失败无提示两处缺陷 |
| 2026-07-26 | E-1 资金对账展示/列表/表单拆分 | 新增 `useFundPresentation`、`useFundList`、`useFundForm`，从资金账户/银行流水双标签页抽离账户名称与类型/状态/收支/对账状态标签及金额格式化、账户与流水查询分页/启用账户选项加载/详情弹窗、建账户与建流水表单及流水匹配-取消匹配（按收支方向锁定业务类型、取消匹配强制填原因）；24 个独立测试，契约门禁改为按视图+三个 composable 整块校验。同时修复查询与切换每页条数未复位分页、账户选项加载失败静默无提示三处缺陷 |
| 2026-07-26 | E-1 生产 BOM 展示/列表/表单拆分 | 新增 `useProductionBomPresentation`、`useProductionBomList`、`useProductionBomForm`，从 BOM 管理页抽离产品/物料标签与状态展示、查询分页/产品选项/详情打印、新建编辑物料明细（选料水合编码单位、保存前物料行校验）；19 个独立测试，契约门禁改为按视图+三个 composable 整块校验。同时修复分页翻页误重置到第 1 页、产品选项加载失败静默无提示两处缺陷 |
| 2026-07-26 | E-1 工艺路线展示/列表/表单拆分 | 新增 `useProductionRoutingPresentation`、`useProductionRoutingList`、`useProductionRoutingForm`，从工艺路线页抽离 BOM/工作中心标签与状态展示、查询分页/选项加载/详情打印/启停、新建编辑工序明细（保存前工序完整性校验）；16 个独立测试。同时修复切换每页条数误重置查询页逻辑，分页改为独立 page/size 处理器 |
| 2026-07-26 | E-1 请购单展示/列表/表单拆分 | 新增 `usePurchaseRequisitionPresentation`、`usePurchaseRequisitionList`、`usePurchaseRequisitionForm`，从请购单页抽离状态/审批/供应商标签、查询分页/选项/详情打印/提交审批驳回作废转单、新建编辑明细行；15 个独立测试。补齐列表加载失败、选项加载失败与保存失败提示文案 |
| 2026-07-26 | E-1 异常工单展示/列表/表单拆分 | 新增 `useExceptionTicketPresentation`、`useExceptionTicketList`、`useExceptionTicketForm`，从异常工单页抽离状态/优先级/分类/事件标签与汇总卡片、查询分页、新建与分派/开始/解决/关闭动作；13 个独立测试。补齐列表加载、创建与动作失败提示，分页改为独立 page/size 处理器 |
| 2026-07-26 | E-1 异常规则展示/列表/表单拆分 | 新增 `useExceptionRulePresentation`、`useExceptionRuleList`、`useExceptionRuleForm`，从异常规则页抽离类型/优先级/扫描状态标签与汇总卡片、规则与命中双列表查询分页、单条/全部扫描与启停、配置阈值表单；11 个独立测试。补齐规则/命中加载、扫描、启停与保存失败提示 |
| 2026-07-26 | E-1 异常 SLA 策略展示/列表/表单拆分 | 新增 `useExceptionSlaPolicyPresentation`、`useExceptionSlaPolicyList`、`useExceptionSlaPolicyForm`，从 SLA 策略页抽离分类/优先级标签与汇总卡片、查询分页、配置时限/升级表单；9 个独立测试。补齐列表加载与保存失败提示，分页改为独立 page/size 处理器 |
| 2026-07-26 | E-1 系统用户展示/列表/表单拆分 | 新增 `useSystemUserPresentation`、`useSystemUserList`、`useSystemUserForm`，从用户管理页抽离部门/岗位/角色/状态标签、查询分页/启停/重置密码、新建编辑与角色分配/数据范围配置；13 个独立测试，契约门禁改为按视图+三个 composable 整块校验。同时修复分页翻页/改页大小处理 |
| 2026-07-26 | E-1 补货建议展示/列表/表单拆分 | 新增 `useInventoryReplenishmentPresentation`、`useInventoryReplenishmentList`、`useInventoryReplenishmentForm`，从补货建议页抽离履约状态标签与数量时间格式化、查询分页/选项/取消转采购、草稿编辑表单；7 个独立测试。选项分页统一为 `pageSize=200`，分页改为独立 page/size 处理器 |
| 2026-07-26 | E-1 工作中心展示/列表/表单拆分 | 新增 `useProductionWorkCenterPresentation`、`useProductionWorkCenterList`、`useProductionWorkCenterForm`，从工作中心页抽离状态标签、查询分页/打印/启停、新建编辑表单；6 个独立测试，分页改为独立 page/size 处理器 |
| 2026-07-26 | E-1 预生产验收展示/列表/表单拆分 | 新增 `useReadinessPresentation`、`useReadinessList`、`useReadinessForms`，从预生产验收页抽离预检/运行/验收项/决策标签、预检与运行列表详情/登记预检、新建运行与验收项/证据/结果/决策五类对话框（含 GO 阻塞门禁）；13 个独立测试，契约门禁改为按视图+三个 composable 整块校验。同时修复分页 page/size 独立处理 |
| 2026-07-26 | E-1 系统角色展示/列表/表单拆分 | 新增 `useSystemRolePresentation`、`useSystemRoleList`、`useSystemRoleForm`，从角色管理页抽离状态/仓库标签、查询分页/启停、新建编辑与菜单授权/数据范围配置；12 个独立测试，契约门禁改为按视图+三个 composable 整块校验。同时修复分页翻页误走查询重置 |
| 2026-07-26 | E-1 系统日志展示/列表拆分 | 新增 `useSystemLogPresentation`、`useSystemLogList`，从系统日志页抽离执行耗时/成功态/JSON 格式化与操作/登录/审计三标签查询分页详情导出；9 个独立测试，契约门禁改为按视图+两个 composable 整块校验。同时修复三标签分页 size/page 独立处理 |
| 2026-07-26 | E-1 系统配置展示/列表/表单拆分 | 新增 `useSystemConfigPresentation`、`useSystemConfigList`、`useSystemConfigForm`，从系统配置页抽离状态与整数校验、配置/编号规则双标签查询分页启停、配置与编号规则新建编辑表单；10 个独立测试，契约门禁改为按视图+三个 composable 整块校验。同时修复双标签分页误走查询重置，并补齐加载/保存失败文案 |
| 2026-07-26 | E-1 系统字典展示/列表/表单拆分 | 新增 `useSystemDictPresentation`、`useSystemDictList`、`useSystemDictForm`，从字典管理页抽离状态标签与类型过滤、字典类型/字典项加载启停、类型与字典项新建编辑表单；7 个独立测试，契约门禁改为按视图+三个 composable 整块校验。字典类型加载分页改为 `pageNo/pageSize=200` |
| 2026-07-27 | E-1 工作台展示/数据拆分 | 新增 `useDashboardPresentation`、`useDashboardData`，从工作台抽离待办图标优先级金额时间格式化与快捷入口、运营看板/账龄加载与失败回落空态；图表 ECharts 生命周期仍留页面；5 个独立测试 |
| 2026-07-27 | E-1 单据追踪展示/列表拆分 | 新增 `useBusinessTracePresentation`、`useBusinessTraceList`，从单据追踪页抽离汇总卡片/状态标签与关键字检索、业务动态抽屉分页与备注提交；`normalizeTraceRoute` 保留契约扫描；9 个独立测试，契约门禁改为按视图+两个 composable 整块校验，并补齐加载/动态/备注失败文案 |
| 2026-07-27 | E-1 初始数据导入展示/列表拆分 | 新增 `useSystemImportPresentation`、`useSystemImportList`，从导入页抽离类型/状态标签与可提交判定、任务查询分页/模板下载/CSV 预览/详情/错误导出/提交；5 个独立测试，契约门禁改为按视图+两个 composable 整块校验，并补齐加载/预览/提交失败文案 |
| 2026-07-27 | E-1 审批待办展示/列表拆分 | 新增 `useWorkflowTaskPresentation`、`useWorkflowTaskList`，从审批待办页抽离业务类型/状态标签与时间格式化、查询分页/详情/通过驳回/转签升级；路由种子与 watch 仍留页面，分页改为独立 page/size 处理器；补齐加载/详情/审批/驳回/升级失败文案；9 个独立测试，契约门禁改为按视图+两个 composable 整块校验 |
| 2026-07-27 | E-1 审批配置展示/表单拆分 | 新增 `useWorkflowConfigPresentation`、`useWorkflowConfigForm`，从审批配置页抽离业务类型/用户角色标签与节点审批人表单（加载/校验/保存）；修复校验参数 `node`→`index` 与 i18n 键对齐，补齐加载/选项/保存失败文案；6 个独立测试，契约门禁改为按视图+两个 composable 整块校验 |
| 2026-07-27 | E-1 销售/采购价目展示/列表拆分 | 新增 `useSalesPricePresentation`、`useSalesPriceList`、`usePurchasePricePresentation`、`usePurchasePriceList`，从销售价目与采购价目页抽离适用范围/状态金额展示与查询分页/新建编辑/启停打印；Element 表单校验仍留页面，业务规则（客户/供应商必选、最低价/最高价边界）下沉 composable；补齐加载/选项/保存/启用失败文案；8 个独立测试 |
| 2026-07-27 | E-1 销售报价展示/列表/表单拆分 | 新增 `useSalesQuotePresentation`、`useSalesQuoteList`、`useSalesQuoteForm`，从销售报价页抽离状态金额日期展示、查询分页/确认作废/打印详情/转销售订单、新建编辑明细表单；补齐选项/详情/保存/确认/作废/转单失败文案与分页 size 处理；9 个独立测试，i18n 契约改为按 presentation composable 校验金额日期格式化 |
| 2026-07-27 | E-1 发票登记展示/列表/表单拆分 | 新增 `useFinanceInvoicePresentation`、`useFinanceInvoiceList`、`useFinanceInvoiceForm`，从发票登记页抽离类型状态金额展示、查询分页/打印/确认作废、新建编辑表单；分页改为独立 page/size 处理器；8 个独立测试 |
| 2026-07-27 | E-1 会计科目展示/列表/表单拆分 | 新增 `useAccountSubjectPresentation`、`useAccountSubjectList`、`useAccountSubjectForm`，从会计科目页抽离类别状态展示、树/筛选列表与启停、根/下级/编辑表单；去掉页面 console.error；9 个独立测试 |
| 2026-07-27 | E-1 序列号台账展示/列表/表单拆分 | 新增 `useInventorySerialPresentation`、`useInventorySerialList`、`useInventorySerialForm`，从序列号台账页抽离仓位状态标签、查询出库报废、仅序列号商品建档表单；7 个独立测试 |
| 2026-07-28 | E-1 系统菜单展示/列表/表单拆分 | 新增 `useSystemMenuPresentation`、`useSystemMenuList`、`useSystemMenuForm`，从菜单管理页抽离类型/状态标签与上级树、树加载启停、新建子菜单/编辑表单；8 个独立测试，契约门禁改为按视图+三个 composable 整块校验 |
| 2026-07-28 | E-1 报表中心展示/列表拆分 | 新增 `useReportPresentation`、`useReportList`，从报表中心抽离五类报表标签/汇总/展示映射与独立分页状态、查询参数、加载导出；页面脚本从 239 行降至 75 行，补齐加载失败双语反馈与 7 个独立测试 |
| 2026-07-28 | E-1 系统部门展示/列表/表单拆分 | 新增 `useSystemDeptPresentation`、`useSystemDeptList`、`useSystemDeptForm`，从部门管理页抽离状态/上级树展示、树加载与启停、新建子部门/编辑表单；7 个独立测试，契约门禁改为按视图+三个 composable 整块校验 |
| 2026-07-28 | E-1 系统岗位展示/列表/表单拆分 | 新增 `useSystemPostPresentation`、`useSystemPostList`、`useSystemPostForm`，从岗位管理页抽离状态标签、查询分页/启停、新建编辑表单；分页改为独立 page/size 处理器并改用 `pageNo/pageSize`；9 个独立测试；契约门禁改为按视图+三个 composable 整块校验，并同步修复此前已拆分页面因契约未组合 composable 导致的误报 |
| 2026-07-27 | E-1 总账明细账展示/列表拆分 | 新增 `useFinanceLedgerPresentation`、`useFinanceLedgerList`，从总账/明细账页抽离金额日期合计与科目树查询、总账/明细分页、导出与下钻；去掉 console.error；分页改为独立 page/size 处理器；5 个独立测试 |

---

## 7. 相关

`WHAT_IS_MISSING.md`、各功能日切文档、`archive/2026-06-stale/`
